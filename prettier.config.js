module.exports = {
  tabWidth: 2,
  // 行尾需要有分号
  semi: true,
  // 使用单引号代替双引号
  singleQuote: true,
  // 末尾使用逗号
  trailingComma: 'none',
  // 使用默认的折行标准
  proseWrap: 'never',
  // 根据显示样式决定 html 要不要折行
  htmlWhitespaceSensitivity: 'strict',
  // 换行符使用 lf
  endOfLine: 'auto'
};
